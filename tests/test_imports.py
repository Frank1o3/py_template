def test_package_imports() -> None:
    """
    Smoke test to ensure the package is installed and importable.
    This mainly catches renaming or Poetry packaging issues.
    """
    import project_name
